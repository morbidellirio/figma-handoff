Use screens/home/frame.json as the structural source of truth.
Use screens/home/reference.png as the visual source of truth.

Do not redesign the UI.
Do not simplify spacing, typography, radii, or sizing.
Rebuild the screen in React + Tailwind.
For the first pass, target a fixed 390px mobile viewport.
Use reusable components only if repeated patterns are clearly present.
Use local files from this repository instead of temporary remote URLs.